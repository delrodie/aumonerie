{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.annee) }}
    </div>
    <div class="form-group">
        {{ form_row(form.theme) }}
    </div>
    {{ form_widget(form) }}
    <div class="text-right">
        <a href="{{ path('programme_index') }}" class="btn btn-default">Annuler</a>
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
