<form method="post" action="{{ path('programme_activite_delete', {'id': programme_activite.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette activité?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programme_activite.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
